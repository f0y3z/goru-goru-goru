# intrucitons to test this setup
```bash
sudo apt update
sudo apt install openjdk-17-jdk
```

use the run.sh file to test the code
first run this to make the sh file executable
```bash
chmod +x run.sh
```
***then run***
```bash
./run.sh
```

